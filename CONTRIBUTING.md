# Contributing

Thanks for your interest! PRs welcome.

## Dev Shell

To enter the dev shell:

```console
nix develop
```

When in the dev shell, you can run tests with `pytest`. This does not include the end to end tests.

## Tests

To run all test (including the end to end tests):

```console
nix flake check
```
